# ARCHIVED — Coordination Status (July 2026)

> Historical multi-repository coordination record. It is not current deploy
> status and must not be updated or used as an instruction source.

# STATUS — worker-owned. Update + push after every milestone, blocker, or question.

## Current state (2026-07-03, newest facts up top)
- **E2-A2 (transport token-resolve read) — LANDED (commit `d5efad2`). NEW CONTRACT sha `280b05d4…` — needs your verify + broadcast.** `GET /v1/comms/resolve-reply-address?address=<token>` — transport-only, account-agnostic (outside `/accounts/*`, `requireAuth` per-route). Two-layer uniform-404 fencing: (1) the binding read runs under the caller's OWN RLS, so tokens of accounts the transport doesn't serve are invisible outright; (2) the caller must hold `role='agent'` in the resolved account — landlord probes, foreign transports, unknown and revoked tokens are all the same 404 (no oracle). Address trim+lowercased like capture. Returns `{account_id, thread_id, participant_id}` for an ACTIVE email binding. **Bonus ask included:** threads LIST gains a `channel` filter. Delta vs `0f0cb770…`: exactly 1 path (+`CommResolveReplyAddressResponse`) + 1 query param — nothing else touched. 5 new checks (28 total in `test:comms-email-threads`): happy path w/ case normalization + exact ids, landlord 404, foreign-account agent 404 (fresh transport identity serving only account B), unknown + deactivated-binding 404, list filtering. All gates green (check chain incl. drift×2 + guide 133 rows, build, unit 58, comms 37 / group 24 / email 18 / email-threads 28, api-isolation). NOT on prod; rides with the pending email migrations.
- **E2-A — DONE.** Verified + broadcast; `sender_mismatch` RATIFIED in full (B's transport rule set: no relay, no reply, poke fires). Deploy notes recorded (migration `…0002` joins the pending migrate:up; `EMAIL_REPLY_DOMAIN` + MX routing on the enablement checklist).
- **E2-A (email inbound + email threads) — LANDED (commit `cc7ca02`). CONTRACT sha `0f0cb770…` — VERIFIED + broadcast.** All four parts shipped: migration `20260703000002_comms_email_threads.sql` + per-(thread,participant) tokenized reply routing + sender verification (`sender_mismatch`) + channel-aware threads. Token/binding mechanics for B and two flagged decisions in "E2-A — LANDED" below. NOT on prod; migration + `EMAIL_REPLY_DOMAIN` env pending.
- **Previously standing by — E1-A remains DONE** (verified + broadcast; mailer deletion still awaits your cutover-verified signal). E1-A VERIFIED + broadcast by the coordinator (both flagged items — the `author_type` widening and the scan `channel` filter — APPROVED); E1-A is DONE. Remaining triggers I'm waiting on: (a) a new INBOX work item (email slice 2 / inbound is held), (b) the coordinator's **cutover-verified** signal → then I delete `api/src/admin/mailer.ts` + the RESEND/MAIL_FROM env, (c) any post-deploy verification ask. Human-side (via coordinator): E1-A migration `20260703000001` joins the next prod `migrate:up`; `UNSUBSCRIBE_HMAC_SECRET` set identically in both services; `COMMS_EMAIL_PIPELINE` stays OFF until B's email driver is verified live.
- **✅ PR #50 POST-DEPLOY VERIFIED — 2026-07-03T01:34:47Z. Render serves the GM contract `304e32c2…`.** `GET https://rental-crm-api.onrender.com/openapi.json` → HTTP 200, 407,005 bytes. Live raw sha `c739246e0fa50c385f01c8ba1560675e61d379c4d1ad177cc6465f03a8f300b4` == sha256 of the MINIFIED committed doc (same pretty-vs-minified nuance as PR #49 — NOT a mismatch); order-insensitive semantic equality vs committed `openapi/openapi.json` (`304e32c2…`) = **True**. GM-A spot-checks on the LIVE doc: `CommThread.mode` enum `[bridged,group]` ✓, `CreateCommThreadBody.mode` default `bridged` ✓, `CommOutbox.group_addresses` `[array,null]` ✓, `CaptureCommInboundBody.cc` present ✓, `CommOutbox.status` clean (no null) ✓, 107 path templates. Nothing 500s from the unauthenticated surface; FE's authenticated structural check is the remaining live probe.
- **E1-A (email slice 1, send-only) — DONE (commit `7bd6f73`; sha `d6adb2b9…` VERIFIED + broadcast).** All four parts shipped: migration `20260703000001_comms_email_slice1.sql` + contract additions + public unsubscribe endpoint + renewal cutover flag (OFF by default; mailer intact until the cutover-verified signal). Full details, the transport-facing token format, and the journal shape in "E1-A — LANDED" below. NOT on prod yet; migration pending the next prod `migrate:up`.
- **GM-A — DONE.** Verified + all six decisions ratified; sha `304e32c2…` broadcast; single prod `migrate:up` (…05 + GM-A together) approved and applied by the human before PR #50. Bridged + group comms now LIVE in prod (dormant pending the enablement checklist: platform numbers, transport flags, Telnyx STOP check, `prune-inbound-raw` cron).
- **Working tree clean**; nothing of mine uncommitted. (Two untracked pre-session files — `twilio_tables_backup_20260626.sql`, `.agents/` — are stray local cruft, deliberately NOT committed.)

## ✅ LIVE DEPLOY VERIFIED — 2026-07-02T23:10:44Z (deploy DoD met — coordinator ACCEPTED)
Ran the live check from my machine (public endpoint, no creds — my env allows this even though prod-DB creds are blocked). `GET https://rental-crm-api.onrender.com/openapi.json` → HTTP 200, 406,695 bytes. Render has deployed main (`32c24dc` / PR #49).
- **All 17 `/comms/*` operations are LIVE** (13 path templates): outbox create/scan/get/complete/fail/delivery, inbound, opt-outs GET+POST, threads GET+POST + `{id}` + `{id}/messages`, policies GET+POST + revoke, reconcile.
- **Contract is byte-identical to the committed spec** — semantic (order-insensitive) equality against `openapi/openapi.json` = **True**. Spot-checks: `CreateCommOutboxBody.tenancy_id` present (item-3 additive field deployed); `CommOutbox.status` enum has NO null (the corruption fix is live).

**⚠️ sha nuance — READ before you curl-compare:** the live raw `sha256sum` is **`7a4b764a0f1d5cbd…`**, NOT `7143b97f…`. This is NOT a mismatch. `7143b97f` is the sha of the committed *pretty-printed* file (`JSON.stringify(doc, null, 2) + "\n"`); the runtime serves via Hono `c.json()`, which is *minified* (`JSON.stringify(doc)`, no indent/newline). I verified `sha256(JSON.stringify(committed_doc)) == 7a4b764a` == the live sha, and that the parsed contract is identical. So the served contract is exactly the frozen one — the two shas just reflect pretty-vs-minified formatting of the same document. (If you want a curl-able target, `7a4b764a0f1d5cbd3283510ebf6cbaef04efc6226608edd6a38e7a1c4739e5b6` is the live raw sha.)

**Deploy DoD: MET** (coordinator ACCEPTED the pretty-vs-minified analysis + recorded the raw live sha). Bridged comms is live in prod, **dormant pending the enablement checklist** (platform numbers, transport flags, Telnyx STOP check, prune cron).

## E2-A — LANDED 2026-07-03 (commit `cc7ca02`; sha `0f0cb770…` awaiting your verify + broadcast)
Migration `20260703000002_comms_email_threads.sql` + `comms.ts` + `api/test/comms-email-threads.test.ts` (23 checks, CI: `test:comms-email-threads`). Your four parts:

1. **Bridged email threads — 501 lifted.** `channel:'email'` + `mode:'bridged'` creates; voice stays 501; group email stays 501 (+ DB CHECK `comm_threads_group_sms_only` backstop). `comm_threads` gains `channel` (frozen) and an optional email-only `subject` SEED (frozen; "Re: " continuation is B's rendering, per your note — thread legs carry NO outbox subject of their own). Landlord participant: a `landlord_user` WITH an email is REQUIRED (they reply natively from their own inbox); address resolution: explicit → the account's `channel_identities` → (participant IS the caller) the caller's JWT email — both tiers tested. Unset `EMAIL_REPLY_DOMAIN` (new env, global receiving domain, render.yaml `sync:false`) → 503.
   **BINDING MECHANICS (FOR B):** I went with a dedicated column, not platform_number reuse (platform_number is FK-pinned to the account's provisioned numbers — a minted token address doesn't belong there). `thread_channel_bindings` gains `channel` ('sms'|'email', trigger-STAMPED from the thread, forge-proof + frozen) and `reply_address` — the FULL minted token address, `t-<32 lowercase hex chars>@<EMAIL_REPLY_DOMAIN>` (128-bit random, minted at creation, frozen, partial-UNIQUE among active bindings — THE email routing key). Shape CHECKs both ways: sms rows carry platform_number and no reply_address; email rows the reverse (platform_number is now nullable at the column level). Email rows are structurally exempt from the sms routing invariant (`(platform_number, participant_address) WHERE active AND thread_mode='bridged'` — a NULL never collides), so one person can hold an sms thread AND any number of email threads; there is deliberately NO per-set uniqueness for email threads (tokens disambiguate). EVERY participant (landlord included) gets a token; B sets it as the From/Reply-To on the relayed mail for that participant. Thread detail/list expose `channel` + `reply_address` on bindings for B/C.
2. **capture_inbound (email).** Same 10-arg signature; `p_channel='email'` routes by the token in `p_to_number` (the handler trims+lowercases BOTH `to_number` and `from_address` for email — bound addresses and tokens are stored lowercased; B should still send them clean). Token resolved + sender matches the bound participant → `matched` (journal attribution exactly like sms). No active token match (or wrong account — pinned) → `orphan`, raw-captured only. `cc` has NO email semantics in v1 and is ignored for routing.
   **⚠️ FLAGGED DECISION (sender-mismatch handling — ratify or redirect):** a resolved token whose from-address is NOT the bound participant's journals INTO the thread (the contact happened at that thread's unique address — suppressing it would hide evidence) with `party_type='unspecified'`, `party_id=null`, `party_label=<the actual sender address>`, and returns NEW disposition **`sender_mismatch`** — the transport must NOT auto-relay it as verified (B's policy call what to do beyond that). Second half of the decision: `author_type` stays the bound slot's capacity (e.g. 'tenant' for a tenant's token) — the message entered that participant's channel; the PARTY fields carry the identity doubt. Rationale: people legitimately reply from aliases/forwarders, so hard-orphaning would drop real tenant mail into a void; the mismatch is surfaced honestly instead. Mismatch wins over opt-out in the disposition (transport won't reply either way).
3. **Relay — zero changes needed, verified.** Email relay legs ride the existing machinery end-to-end (tested): agent intent w/ `thread:<id>` provenance + `relay_of_interaction_id`, destination resolved from the (thread, participant) binding (the participant's real email, not the token), `normalizeAddress('email')` validates, and `complete_send` links the ORIGINAL interaction — journal-once held.
4. **Spec (additive + two widenings, fully enumerated):** `CommThread` +`channel` +`subject`; `CommThreadBinding` +`channel` +`reply_address`, and **`platform_number` → nullable** (email bindings return null there — same honest-contract move as the E1-A author_type widening; B/C re-pin); `CreateCommThreadBody` +optional `subject`; `CaptureCommInboundResponse.disposition` enum +`sender_mismatch`; `CaptureCommInboundBody.to_number` maxLength 50→320 (a token address exceeds a phone number; pure relaxation). No path/schema added or removed; nothing narrowed.

**Tests (23, all green):** tokenized create (unique `t-…@domain` per participant incl. landlord, lowercased, platform_number null); the create guards (no-landlord 400, agent 400, dup addresses 400, subject-on-sms 400, voice 501, group-email 501); BOTH landlord resolution tiers (JWT default when no identity; identity outranks JWT after one is stored); token capture matched + case/space normalization; sender_mismatch (journal shape asserted + idempotent replay); landlord-token capture; orphan; cross-account pinned orphan; email relay journal-once; channel-aware thread message (email leg, no subject); sms/email coexistence (both directions route to their own thread); email opt-out (422 send + `opted_out` inbound still journaled). Plus a 14-check direct-SQL smoke during development. One legacy update: comms.test.ts's F7 check now asserts voice→501 and email→typed 503 when `EMAIL_REPLY_DOMAIN` is unset (email became a real path).

**Gates:** full `pnpm check` (drift×2, guide 132 rows), build, unit 58, comms 37 + group 24 + email 18 + email-threads 23, interactions-journal / agent-principal / events-feed / api-isolation, db isolation (54) / audit / money / tenancy / comms-retention on the full chain, definer-grant guard, admin-quarantine.

**Deploy note (via you, for the human):** migration `20260703000002` joins the pending prod `migrate:up` (expand-only, safe with deployed code); `EMAIL_REPLY_DOMAIN` must be set in Render (and its inbound mail routed to B's webhook — ops) before any email thread is created in prod.

## E1-A — LANDED 2026-07-03 (commit `7bd6f73`; sha `d6adb2b9…` VERIFIED + broadcast)
Migration `20260703000001_comms_email_slice1.sql` + `comms.ts`/new `unsubscribe` route + `api/test/comms-email.test.ts` (18 checks, CI: `test:comms-email`). Your four parts:

1. **`subject`** — `comm_outbox.subject` (1..998, email-only DB CHECK, frozen at intent like every intent field). `CreateCommOutboxBody.subject` optional (API 400 + fieldErrors on any non-email channel); `CommOutbox.subject` nullable in responses. **Journal shape (FOR B's TEMPLATE RENDERING — the documented contract):** `complete_send` journals an email row with a subject as
   `body = 'Subject: ' + subject + '\n' + '\n' + body`
   (literally the string `Subject: `, the subject, one blank line, the body — nothing else added). Emails without a subject journal the body alone. `interactions.body` is unconstrained, so no truncation ever.
2. **`system:<flow>` provenance** — `comm_outbox.author_type` gains `'system'`, DB CHECK-paired 1:1 with `approval_ref LIKE 'system:%'` (both or neither), `approved_by` null. The capacity trigger rejects the pair from ANY JWT-bearing caller (member or agent, PostgREST or API — 23514); the API additionally 403s `system:` refs from both principals up front. Only core's service tier (admin client) can mint one — enforced, tested (API 403 both principals + raw-PostgREST forge rejection). First user: `approval_ref='system:capture_renewal'` (the inspection-capture renewal email — its first-ever journal record). ⚠️ One deliberate response-contract change beyond pure addition: **`CommOutbox.author_type` enum widened to `['landlord','agent','system']`** — B's strict response validation would otherwise choke on system-authored rows in the dispatch scan the moment the cutover flag turns on (the exact spec-vs-served class the `f3336606` corruption taught us to pre-empt). Widening only; requesting it via the API stays impossible.
3. **Unsubscribe (CAN-SPAM + RFC 8058)** — public, unauthenticated, magic-link pattern (per-IP `bump_ip_rate_bucket` scope `unsubscribe`, 120/10min, fail-open; uniform 404 for malformed AND bad-sig tokens — no oracle; 503 if the secret is unset).
   **TOKEN FORMAT v1 (FOR B — mint statelessly, no round trip):**
   - `address_lc` = trim + lowercase of the recipient email
   - `mac` = HMAC-SHA256(key = `UNSUBSCRIBE_HMAC_SECRET` as utf8, message = `'unsub:v1:email:' + address_lc`) — full 32 bytes
   - `token` = `base64url_nopadding(utf8(address_lc)) + '.' + base64url_nopadding(mac)`
   - URL: `https://<api-host>/v1/unsubscribe/email/<token>`; headers B sets: `List-Unsubscribe: <URL>` and `List-Unsubscribe-Post: List-Unsubscribe=One-Click`
   - `GET` registers the opt-out immediately + renders a minimal confirmation page (CAN-SPAM single-visit honoring); `POST` is the one-click (no body required or parsed; returns `{status:'unsubscribed'}`). Both idempotent (`record_opt_out` upsert; the RPC gained an explicit service-tier allowance — auth.uid() NULL can only be the service key or a direct connection since anon/PUBLIC hold no EXECUTE and every authenticated JWT carries sub). Registration parks queued email intents to the address and refuses new ones at 422, same as SMS STOP.
   - New env (both in render.yaml, `sync:false`): `UNSUBSCRIBE_HMAC_SECRET` (min 32 chars, SHARED with B — the human must set the same value in both services; unset → endpoint 503s and B must not emit List-Unsubscribe headers).
4. **Renewal cutover (flag, mailer intact)** — env `COMMS_EMAIL_PIPELINE` (`'on'`/`'true'` = on; default OFF): ON → `requestCaptureRenewal` writes ONE `comm_outbox` intent via the admin client (channel email, lowercased on-file address, subject `Your condition form link`, same body text, `system:capture_renewal`, `author_type='system'`, the inspection's `tenancy_id` as journal context) — fire-and-forget exactly like the old mailer call, so the anti-enumeration contract (uniform 202, no correlated latency) is preserved; a P0004 opt-out refusal is logged as compliant suppression, never an error. OFF → the legacy mailer path byte-for-byte. `api/src/admin/mailer.ts` + RESEND/MAIL_FROM env stay until you signal cutover-verified; then I delete.

**Also additive in this emit:** `GET /comms/outbox` dispatch scan gains an optional `channel` filter (B runs separate per-channel dispatch loops; scoping the scan server-side beats client-side filtering). Flag if unwanted.

**Contract delta (`d6adb2b9…` vs `304e32c2…`), fully enumerated:** +path `/v1/unsubscribe/email/{token}` (GET html + POST json; +`UnsubscribeResponse` schema); `CommOutbox` +`subject` (required-in-response, nullable) + the author_type widening above; `CreateCommOutboxBody` +optional `subject`; outbox scan +`channel` param. Nothing removed, nothing narrowed, no other schema touched.

**Tests (18, all green):** subject accept/echo + sms 400; claim→complete → journal `Subject: …\n\n…` email/outbound; system: 403 for agent AND landlord (+ no row) + raw-PostgREST forge rejected; one-click POST (no auth/body) 200 → queued intent parked + new intent 422; GET page contains the address + registers; POST replay idempotent; tampered mac 404 + garbage 404; scan channel=email/sms filtering; renewal → 202 + intent row (address/subject/provenance/tenancy asserted, polled past the fire-and-forget); renewal to an opted-out address → 202 uniform + NO row; outbox GET carries subject. Plus a 10-check direct-SQL smoke of the DB rules during development (incl. a simulated member-JWT forge of system:).

**Gates:** full `pnpm check` (incl. drift×2, guide 132 rows), build, unit 58, comms 37 + group 24 + email 18, interactions-journal / agent-principal / events-feed / api-isolation, db isolation (54 tables) / audit / money / tenancy / comms-retention on the full chain, definer-grant guard, admin-quarantine.

**Deploy note for the human (via you):** the E1-A migration `20260703000001` joins the pending prod `migrate:up`. It is expand-only and safe with the currently-deployed code. The two new env vars need setting in Render before (or with) the flag flip; `COMMS_EMAIL_PIPELINE` stays OFF until B's email driver is live.

## GM-A — LANDED 2026-07-02 (commit `d580c27`; sha `304e32c2…` VERIFIED + broadcast; LIVE in prod via PR #50)
All six directive parts, mapped to what shipped. Migration `20260702000001_comms_group_mms.sql` + `comms.ts` + `api/test/comms-group.test.ts` (CI: `test:comms-group`, step added after `test:comms`).
1. **`comm_threads.mode`** `'bridged'|'group'` (default bridged, additive). `CreateCommThreadBody.mode` optional (spec default `bridged`); `CommThread` response exposes `mode` (required — additive for consumers). Group threads: sms-only (existing 501 covers), participants must include ≥1 `landlord_user` WITH an address (their phone IS a group member and gets bound), tenant/vendor need `party_id`, `agent` participants rejected 400, member addresses pairwise-distinct, set size 2..7 (8 incl. our number).
2. **Group-set uniqueness — implemented STRONGER than "create path": DB-enforced and race-free.** New internal column `comm_threads.group_routing_key` = canonical member-set identity (`<platform_number> + '>' + sorted-deduped member addresses`, our number stripped; SQL helper `_comm_group_routing_key` and the TS create path compute it in lockstep — a capture test locks them together). Partial-unique index over ACTIVE group threads → identical set on one number = 409 regardless of participant order; closing a thread frees the slot. Key + mode are trigger-frozen. The key is INTERNAL (thread reads now use an explicit column list so it never rides into responses). Bindings: per-participant rows kept; new trigger-stamped `thread_channel_bindings.thread_mode` (never trusted from the writer — forge test included) lets the `(platform_number, participant_address) WHERE active` partial-unique bind BRIDGED legs only, so one person can hold a 1:1 thread AND group threads on one number (coexistence + regression both tested).
3. **`capture_inbound` gains trailing `p_cc text[] default null`** (signature change = drop+recreate; allowlist keys by name, guard green; the default keeps the deployed 9-arg transport call working, so the migration is safe ahead of the code deploy). cc present+non-empty → account-pinned set-match on the routing key; sender's active binding supplies the journal participant; no match → orphan (raw-captured only). No cc → 1:1 binding path unchanged (now explicitly `thread_mode='bridged'`). `CaptureCommInboundBody.cc` optional, E.164-exact match semantics identical to `from_address` (no normalization — matches the existing 1:1 contract with the transport).
4. **Group outbound = ONE outbox row.** `comm_outbox.to_address` now nullable (response schema already declared it nullable — no shape change) + new `comm_outbox.group_addresses text[]` (nullable, additive): the recipient set FROZEN at intent time from the thread's active bindings (landlord's own phone included — the provider group session is keyed by the full member set), sorted, 2..7. DB CHECK: exactly one destination shape (`to_address` XOR `group_addresses`); capacity trigger backstops the cross-table half for ALL inserters (group rows only into group threads, no participant leg, no relay — "relays do not exist in group mode"; group threads accept ONLY group rows; raw-PostgREST forge test included). Paths: `POST /threads/{id}/messages` on a group thread → one row; `POST /comms/outbox` with a bare group `thread_id` (no to_address/participant_ref/relay → 400 each) works for landlord (`self:`) and agent (`grant:`/proposal) provenance unchanged. `complete_send` journals once on the non-relay path (thread context copied as before); group party attribution = `party_type='unspecified'`, `party_id` null, `party_label` = the dialed set (honest evidence of exactly who was reached). `provider_sid` stores the group_message_id; sid replay idempotency unchanged.
5. **Any-member STOP (hard rule).** Opt-out refuse trigger now refuses when ANY of `group_addresses` is registered (422 `opted_out` at intent create — refused sends leave no journal trace); `record_opt_out` parking UPDATE gains the group arm (queued group rows whose set contains the address park `undeliverable`/`opted_out`, globally). Inbound from an opted-out member still captures + journals (evidence), disposition `opted_out` as before.
6. **Spec: strictly additive.** NEW sha `304e32c2ea9a2ce281ac891e59742c9a31bbd14213e5209b4ee9d6e7868234f2` (supersedes `7143b97f…`). Diff = exactly 4 additions: `CommThread.mode`, `CreateCommThreadBody.mode` (default bridged), `CommOutbox.group_addresses`, `CaptureCommInboundBody.cc`. No path/field removed, nothing narrowed, no other schema touched. SDK regenerated; `check:drift` + `check:guide-drift` green; api-guide §8e updated (group-mode paragraph + 4 table-row notes).

**Tests** (all green locally): the 4 you required — set-match capture (matched / order-insensitive / idempotent replay / orphan on partial set / **cross-account pinned** via a second account sharing the transport user), group-set uniqueness (reordered participants → 409; different set → 201), group send journal-once (exactly one outbound journal row, sid replay returns same interaction_id), any-member opt-out (parks the queued group row + 422 on new group sends + 1:1 to a clean member still 201) — plus create-shape guards (no landlord / dup address / agent participant / >7), direct-outbox group rejections, thread `tenancy_id` riding onto the group journal row, bridged coexistence + the rebuilt routing-index regression, and 2 raw-PostgREST forge backstops (group row on a bridged thread rejected; forged `thread_mode` re-stamped). 24 checks, wired into CI.

**Gates:** full `pnpm check` (lint, typecheck, service-role quarantine w/ documented test-file allowlist entry, drift×2), build, unit (58), api suites (comms 37, comms-group 24, interactions-journal, agent-principal, events-feed, api-isolation), db suites on the full migration chain (isolation 54 tables, audit, money, tenancy-status, comms-retention), definer-grant guard, admin-quarantine. Plus a 13-check direct-SQL smoke of the new DB backstops during development.

**Decisions inside the directive's degrees of freedom (flag if any is wrong):**
- Group-set uniqueness DB-enforced via the stored canonical key (above) rather than a read-then-insert check — the create path computes the key, the index makes it race-free.
- `group_routing_key` kept OUT of the contract (internal identity; derivable by the FE from bindings if ever needed).
- Group journal `party_label` = the dialed address set (there is no single counterparty; the set is the honest attribution).
- The landlord's own address is included in every group send's recipient set (provider group sessions are keyed by the full member set; excluding the sender's own line would fork the group on the other members' handsets).
- One behavior nuance from fetch-thread-first ordering on `POST /comms/outbox`: a NONEXISTENT `thread_id` with no `to_address` now 404s (was 400 "provide to_address…"). All real bridged flows unchanged (comms 37 green).
- Group mode rejects `participant_ref`/`to_address`/relay outright rather than allowing 1:1 side-sends inside a group thread — a single-recipient row journaled under a group thread would misrepresent who a message reached. 1:1 to a member still works via a plain address send (or their bridged thread).

## 🔧 FOLLOW-UP: perf + retention migration `…05` — APPROVED; pending human apply + coordinator PR
Post-deploy optimization from a scalability/performance review (user-requested). Migration `20260701000005_comms_perf_and_retention.sql` — **DB-only, no endpoint/schema change, no new sha, zero B/C impact.** Coordinator APPROVED (A/B/C/D all confirmed sound). **Not applied to prod yet**: sequence is human runs one `migrate:up` (safe with deployed code), then coordinator opens+merges the follow-up PR (I do NOT merge).
- **(A)** partial index on `comm_outbox (channel, to_address) WHERE status='queued'` — `record_opt_out`'s parking UPDATE filters those two columns with no `account_id` (compliance is global), so it was seq-scanning the whole outbox on every STOP; now a point lookup.
- **(B)** reordered `comm_outbox_pending_idx` to `(account_id, status, created_at, id)` so the transport dispatch scan is index-ordered (keyset pagination stopped sorting every page); `not_before` is a cheap recheck, reconcile keeps its `(account_id, status='sending')` prefix.
- **(C)** dropped 4 standalone `(account_id)` indexes (comm_outbox, platform_numbers, comm_threads, comm_policies) — each redundant with the table's `unique(account_id, id)`; trims write cost on the hot path.
- **(D)** `prune_inbound_raw(interval default 90d)` — service-role-only janitor pruning the unaudited `inbound_raw` JSONB bloat past a dedupe horizon. Schedule operationally (`cron.schedule('prune-inbound-raw', …)`), NOT in-migration. **Deliberately did NOT prune `comm_outbox`**: it's audit-attached, so a DELETE emits a `hard_deleted` event carrying the full before-image (bigger than the row) — pruning would grow the events chain more than it shrinks the outbox. With (A)/(B) the terminal rows no longer cost the hot queries anything. Bounding events/comm_outbox is a separate partitioning design (tracked follow-up).

Gates: migration applies clean across the full chain; DEFINER-grant guard passes (janitor is service_role-only, un-allowlisted); new `test:comms-retention` (5 checks) wired into CI; comms API suite (37) still green post index-swap; typecheck/lint/drift green.

## ✅ PROD MIGRATION APPLIED — 2026-07-02T22:30:55Z
The human ran `pnpm --filter ./db migrate:up` against prod (my env's classifier blocked the prod creds, as expected, so the human ran the one command). `supabase db push` applied all THREE, in order, and reported **"Finished supabase db push"** with no errors:
- `20260701000002_comms_ledger.sql` ✅
- `20260701000003_comms_ledger_hardening.sql` ✅
- `20260701000004_comms_relay_and_context.sql` ✅

**Verification**: the `supabase db push` output (it lists exactly those three as applied and finishes clean). Prod-DB reads are classifier-denied to me, so I'm relying on the CLI's applied-migrations report rather than an independent `select` — flag if you want an alternative confirmation.

**Over to you (coordinator) for the remaining steps** — per your sequence, YOU create + merge the PR to main (Render auto-deploys), verify `/comms/*` on the live `/openapi.json`, and broadcast the final sha `7143b97f…` to B/C. I will NOT merge or push to main myself. Nothing further from me unless pinged.

## Current milestone
See **"Current state"** at the top of this file for live status. In short: M0–M3 + hardening + relay/context + perf batch all SHIPPED and VERIFIED; bridged comms LIVE in prod (dormant pending enablement); `…05` approved & pending human apply; GM-A (group-MMS) received & not started. The sections below are the historical record of each phase (hardening review, REOPEN batch, contract history) — kept for provenance, not current asks.

## ✅ REOPEN batch (3 items) — LANDED. Ready for your verify + the combined go/no-go.
Your REOPEN + ITEM-3 notes: all three landed in migration `20260701000004_comms_relay_and_context.sql` + handler/spec changes. **NEW sha `7143b97ffb42b0fa811400f70f755747b098ca9c8b8e4d13aac63b885b092b17`** (Item 3 adds two optional fields — see ⚠️ CONTRACT). All gates green (comms 37 checks incl. new relay + context tests; interactions-journal 28; agent-principal 28; events-feed 13; full ephemeral DB suite incl. DEFINER-grant guard + isolation + audit + money; unit 67; drift×2; build; lint).
1. **complete_send relay double-journal — FIXED.** When `relay_of_interaction_id` is set, `complete_send` no longer inserts a journal row: it marks the leg `sent`, links `interaction_id := relay_of_interaction_id`, and returns that id. To keep this correct I also excluded relay legs from `comm_outbox_interaction_id_uniq` (several legs share one original) and from the `interactions_with_chain` delivery join (so the inbound original keeps its own null delivery_status and the view never multiplies rows). Per-leg delivery stays on the outbox via `comm_outbox_relay_idx`. Test: relay completes with no new journal row, returns the original id.
2. **`thread:<thread_id>` relay provenance — ADDED.** Accepted on agent intents IFF `relay_of_interaction_id` is set, the thread is live + account-owned, and the relayed interaction belongs to it. DB provenance CHECK widened to `approved_by IS NOT NULL OR grant:% OR (thread:% AND relay_of_interaction_id IS NOT NULL)`; handler enforces the thread/interaction linkage. `thread_autonomy` untouched (reserved for real AI interjection). **No spec shape change** from this item (approval_ref is a free string) — confirmed. Tests: 403 without a relay / for a foreign thread / interaction-not-in-thread; 201 for a valid relay.
3. **Journal context linkage — ADDED.** Optional `tenancy_id` / `maintenance_request_id` on `comm_outbox` (nullable, composite-FK to the account) + `CreateCommOutboxBody` + `CommOutbox` response; `complete_send` copies them onto the journal row on the non-relay insert path. Landlord thread messages inherit the thread's context automatically. Test: journal row carries the outbox's tenancy_id after completion.

## 🛑 PROD DEPLOY — STILL HOLDING (batch now complete; over to you for the combined go/no-go)
Per your "one combined go/no-go covering the review findings + fixes + both pending migrations" — the batch you were waiting on is now landed and green. **I have NOT touched prod.** When the human confirms:
- There are now **THREE** pending migrations: `…02` (ledger), `…03` (hardening), `…04` (relay/context). `pnpm --filter ./db migrate:up` applies all three in order. All safe ahead of the code deploy (comms tables are brand-new; the only shared touches — nullable `interactions.thread_id`, the `interactions_with_chain` rebuild, the agent-capacity trigger — are backward-compatible).
- Per [[prod-reads-policy]], direct prod-DB connections with prod creds are classifier-denied in my environment, so I almost certainly **cannot run `migrate:up` against prod myself** — expect to hand step 2 to the human (one command). I'll attempt it on your green-light and, if blocked, say so immediately so the human runs it; then I record the outcome per step 3.
- STOP-ack v1 / transport-readable platform-numbers / resolve-by-provider_sid: acknowledged as tracked follow-ups, no action now.

## ⚠️ CONTRACT — current sha `280b05d4…` (E2-A2; needs your verify + broadcast)
**`280b05d4443d7ddb8fc0c75e60b15a76e82a0b4947c87b8523651753fed348c9`** — supersedes `0f0cb770…`. Delta: +path `/v1/comms/resolve-reply-address` (GET, +`CommResolveReplyAddressResponse`), +`channel` query param on the threads LIST. Nothing else changed.

### Earlier — sha `0f0cb770…` (E2-A; VERIFIED + broadcast 2026-07-03)
**`0f0cb770860954f592b357c31415de9707a7f5f9c60663bac386220767110ed1`** — supersedes `d6adb2b9…`. Delta enumerated in "E2-A — LANDED" above: `CommThread` +channel +subject; `CommThreadBinding` +channel +reply_address, platform_number→nullable; `CreateCommThreadBody` +subject; disposition enum +`sender_mismatch`; `to_number` cap 50→320. No paths changed.

### Earlier — sha `d6adb2b9…` (E1-A; VERIFIED + broadcast 2026-07-03)
**`d6adb2b91d5359aaf3cda7d7f794b3b9393a5f48cd1975f8f1a98c9e6b59aa0a`** — supersedes `304e32c2…` (GM-A, which is the LIVE prod contract as of PR #50). Delta enumerated in "E1-A — LANDED" above: +`/v1/unsubscribe/email/{token}` (GET+POST, +`UnsubscribeResponse`), `CommOutbox` +`subject` + author_type enum widened with `'system'` (deliberate, documented — B's strict validation needs it before the cutover flag turns on), `CreateCommOutboxBody` +optional `subject`, outbox scan +`channel` param. Nothing removed or narrowed.

### Earlier — sha `304e32c2…` (GM-A; LIVE in prod)
Strictly ADDITIVE vs `7143b97f…`, exactly 4 fields: `CommThread.mode` (response, required), `CreateCommThreadBody.mode` (optional, default `bridged`), `CommOutbox.group_addresses` (response, nullable array), `CaptureCommInboundBody.cc` (optional array). Verified + broadcast by the coordinator; serving live since PR #50.

### Earlier contract history — sha `7143b97f…` (deployed to prod)
Latest change (REOPEN item 3): `CreateCommOutboxBody` gains optional `tenancy_id` + `maintenance_request_id`; `CommOutbox` (response) gains the same two as nullable. Strictly ADDITIVE — no field removed/renamed, no other schema touched (diff verified). Supersedes `097d9dc8…`. Items 1 + 2 carry no spec shape change.

### Earlier (still relevant): the corruption fix
My `injectSchemaHygiene` pass (the M2 fix for the nullable-enum items) had a reference-aliasing bug: zod-openapi shares ONE enum-array object across every field that reuses a `z.enum`, and my in-place `.push(null)` on the one nullable use (`CommThreadMessage.delivery_status`) leaked `null` into the NON-nullable `CommOutbox.status`, `CommRelayLeg.status`, and the outbox-scan `status` query param. So in the broadcast `f3336606…`, `CommOutbox.status` wrongly emitted `enum:[…,null]` and the SDK typed it `… | null` — a value the API never sends. **B/C consuming `f3336606…` got `CommOutbox.status` as nullable; it is not.** Fixed (replace-don't-mutate + a 9-case unit spec `api/test/schema-hygiene.spec.ts` locking the shared-array case) and re-emitted. **New FINAL sha `097d9dc8a2a79a5fdeaf34c1bcd63a33fc7ab3a94c3c1dba2eef5397d2320797`** — please re-broadcast to B and C. Delta is strictly a NARROWING (status fields lose a spurious `| null`); no field/endpoint added or removed. Doc-wide sweep confirms zero remaining nullable-enum mismatches.

## Checklist
- [x] E2-A2 ✅ — transport token-resolve read + threads channel filter (commit `d5efad2`, sha `280b05d4…`) — awaiting coordinator verify/broadcast; "last core work item before the email PR" per INBOX
- [x] E2-A ✅ — email inbound + threads (migration `20260703000002`, tokenized reply routing + sender_mismatch + channel-aware threads, 23-check CI suite, sha `0f0cb770…`) — awaiting coordinator verify/broadcast + sender-mismatch ratification; migration + `EMAIL_REPLY_DOMAIN` pending prod
- [x] E1-A ✅ — email slice 1 (migration `20260703000001`, subject + system: provenance + HMAC unsubscribe + renewal cutover flag, 18-check CI suite, sha `d6adb2b9…`) — awaiting coordinator verify/broadcast; migration + env vars pending prod
- [x] GM-A ✅ — native group-MMS threads (migration `20260702000001`, 4 additive contract fields, 24-check CI suite, sha `304e32c2…`) — verified + broadcast; LIVE in prod via PR #50
- [x] M0 ✅ — firewall relaxation + 17 comms contract stubs + spec/SDK emitted, all fast gates green
  - [x] Firewall: agent `kind='communication'` allowed IFF `approval_ref` + (`approved_by` OR `grant:`-prefixed ref); 403s kept for corrections and no-provenance free text; 17-branch vitest unit spec + integration cases updated
  - [x] All `/v1/accounts/{accountId}/comms/*` routes as typed zod-openapi stubs (501 `not_implemented` via standard envelope)
  - [x] Spec + SDK emitted; `check:drift`, `check:guide-drift`, lint, typecheck, unit tests, api build all green
- [x] M1 ✅ — migration `20260701000002_comms_ledger.sql`: all 9 tables (force-RLS, ADR-0003 member policies, `_emit_event` audit attachment, composite `(account_id,id)` uniques), `interactions.thread_id` + rebuilt `interactions_with_chain` (delivery join), capacity trigger resurrected + provenance-extended, RPCs `complete_send`/`capture_inbound`/`record_opt_out`/`list_account_opt_outs` (DEFINER, self-defending, allowlisted) + `fail_send`/`update_delivery`/`reconcile_scan` (INVOKER), opt-out enforcement as BEFORE-INSERT trigger, outbox state-machine guard trigger. Local: full migration chain + isolation (incl. meaningfulness) + audit + money + tenancy-status + definer-grants guard ALL GREEN.
- [x] M2 ✅ — all 17 endpoints implemented (RPCs + RLS-scoped queries; transport = agent principal, landlord = owner|manager, viewers 403). Provenance enforced on intent create (grant refs must name a LIVE `comm_policies` row of the account; landlord refs stamped `self:<uid>`); destination resolution explicit-address → thread binding → channel identity; opt-out surfaces as 422 `opted_out`; canonical `rent_reminder` params validated per INBOX (`{days_before, monthly_cap}`, unknown keys 400). Spec + SDK re-emitted; api-guide §8e added; firewall now also requires `external_ref` on agent communications app-side (announced; Plan B unaffected — always sends it).
- [x] M3 ✅ — `api/test/comms.test.ts` (24 checks, wired into CI integration job as `test:comms`): intent→claim→complete atomicity (journal appended EXACTLY once, only on confirmed send; replay idempotent), fail leaves NO journal row + terminal 409, needs_reconcile park/resolve, monotonic delivery (stale callbacks no-op), inbound matched/orphan/opted_out + provider_msg_id idempotency, opt-out parks queued intents + blocks new at the boundary + first-wins replay, thread detail w/ delivery state + message cursor paging, policy revoke parks grant sends + blocks new, reconcile scan, Idempotency-Key replay single-row, principal gating sweep, events-feed surfacing, cross-account 404s, binding partial-unique 409 + no-skeleton cleanup. Firewall unit spec (18 branches) + agent-principal suite updated. ALL local suites green: comms, agent-principal, interactions-journal, events-feed, api-isolation, db isolation/audit/money/tenancy/definer-guard, unit, build, lint, drift×2, service-role, admin-quarantine.

## Hardening review (post-ACK; all findings re-verified by me before fixing)
Two independent adversarial reviewers (one DB-focused, one API-focused); I checked every finding against source, kept the real ones, and dropped/deferred the rest with reasons. All fixes are in migration `…03_comms_ledger_hardening.sql` + `comms.ts`/`idempotency-contract.ts` patches; regression guards added.

DB (all reachable only via a member hitting PostgREST directly — RLS/triggers are the boundary):
- **F1 (HIGH, fixed) — agent author-type laundering.** `comm_outbox` had a `for all` member policy and no agent-capacity guard at INSERT, so an agent could directly insert `author_type='landlord'` and `complete_send` (DEFINER + the verified-write GUC that exempts `_enforce_agent_capacity`) would launder it into a HUMAN-attributed journal communication. The dropped predecessor's completion RPC was INVOKER so its trigger caught this; the DEFINER+GUC generalization dropped the shadow. Fix: BEFORE-INSERT capacity trigger forcing agent inserters to `author_type='agent'` (author_type is already immutable post-insert) + birth-status pinned to `queued`. Guard: raw-PostgREST forge test.
- **F2 (MED, fixed) — cross-account inbound dedupe.** `capture_inbound` replay/re-read keyed on the globally-unique `provider_msg_id` with no account filter → could return account B's cached ids or suppress B's real capture. Fix: stamp+pin `matched_account_id`; a msg id already held by another account raises 409, never leaks. Guard: cross-account capture test (agent transports both accounts).
- **F3/F4 (MED, fixed) — opt-out register metadata leak.** `keyword`/`source_ref` (a provider msg id from whoever first reported the opt-out) leaked cross-account via `list_account_opt_outs` (member-forgeable `channel_identities` intersection) and `record_opt_out` (echoed the pre-existing foreign row). The per-address boolean is inherent (a send attempt reveals it) — accepted; the recording metadata must not cross accounts. Fix: neither RPC returns `keyword`/`source_ref` for a row it didn't create this call. Guard: replay-metadata test updated.
- **F5 (MED, fixed) — cross-account routing DoS.** `thread_channel_bindings.platform_number` FK was to the global `platform_numbers(number)`, not the binding's account (FK check bypasses RLS), so a member could bind another account's number and occupy its global routing slot. Fix: composite FK `(account_id, platform_number)`. Guard: cross-account bind test.
- **F6 (LOW, partial) — state guard was UPDATE-only.** Added birth-status=`queued` enforcement (folded into the F1 trigger). DELETE/other-field UPDATE tightening deferred (own-account only; audit chain already records deletions).

API:
- **Finding 2 (HIGH, fixed)** = the contract corruption above.
- **Finding 3 (fixed)** — `createOutbox` grant validation now also requires the grant's `channel` to match the send's channel (an sms grant no longer authorizes voice/email).
- **Finding 4 (fixed)** — `complete`/`fail`/`delivery` handlers now pin the outbox row to the URL account before the RPC (clean 404 on divergence; RPCs previously self-defended on the row's OWN account only).
- **Finding 5 (fixed)** — `createOutbox` binding-resolved destination now runs through `normalizeAddress` (channel/format validated, not silently reused).
- **Finding 6 (fixed)** — `revokePolicy` concurrent double-revoke now stays idempotent (re-reads on lost race) instead of a spurious 500.
- **Finding 7 (fixed)** — `createThread` now refuses non-sms channels (501) — the message path is sms-only today, so accepting email/voice threads would mis-send. Schema still advertises the future channels.
- **Finding 8 (fixed)** — `is_approver_member` RPC errors are surfaced, not swallowed into a misleading 400.

Considered and NOT changed (with reasons):
- **Finding 1 (agent cites a nonexistent grant on the DIRECT `POST /interactions` path) — DEFERRED to you (Question 4).** That path RECORDS a send that already happened (Plan B's confirmed-send journal). Rejecting it because the cited grant is revoked/absent would suppress the record of a real send — an ADR-0007 violation ("a message is never sent without a record"). Grant validation is enforced at INTENT-CREATION on `/comms/outbox` instead. Also it's a behavior change on Plan B's active path, so per protocol I won't tighten it without your ack.
- Monthly-cap / quiet-hours enforcement, `thread_autonomy`/`voice_autonomy` params: tracked follow-ups (need counting infra / your canonical shapes).
- RPC role-vs-membership checks (`complete_send`/`list_account_opt_outs` check membership not role): documented-intentional in the definer-grant allowlist; consistent with the codebase's "RLS = tenant isolation; role = app-layer" model.

## Contract
- **FINAL spec** (corrected): `openapi/openapi.json` sha256 `097d9dc8a2a79a5fdeaf34c1bcd63a33fc7ab3a94c3c1dba2eef5397d2320797`. Supersedes `f3336606…` (which carried the `CommOutbox.status` nullability corruption — see ⚠️ CONTRACT). Content otherwise identical: the 3 INBOX re-emit items + `CommPolicy.quiet_hours` nullability restoration are all present; doc-wide sweep clean.
- Superseded shas: `f3336606…` (broadcast, corrupted status enums — DO NOT USE), M2-pre-hygiene `b0817547…`, M0 `215ac1e2…` (commit `520ed50`).
- **INBOX 2026-07-02 "M0 addition" — already satisfied in the sha above.** `CreateInteractionBody.external_ref` is writable exactly as requested: agent principal + fresh `kind='communication'` only (landlords 400 `invalid_request`; corrections/notes/agent_events 400 via shape validation). Firewall governs, per your "simplest" option.
- Endpoints: the full PLAN.md M0 list, plus **`GET /comms/outbox/{id}`** (added: the ADR-0007 "send_state_unknown" resolution read the transport needs after a lost complete/fail response; flag if unwanted).
- **`Interaction` gains `thread_id?: uuid|null`** (contract-first: column lands in M1; reads return null/absent until then).
- **`POST /interactions` gains optional agent-only `external_ref`** (kind='communication' only, landlords 400). Rationale: M1 resurrects the DB backstop *"agent-authored communications require external_ref"* — without a body field the direct agent journal path would be structurally dead post-M1. Optional at the app layer in M0; once the M1 trigger lands I intend to enforce it in the firewall too (clean 400 instead of DB error). **Coordinator: please confirm this matches landlord-agent/docs/agent-sends-core-records.md** (not readable from this repo).
- Provenance convention implemented exactly as specified: `proposal:<id>`+approved_by / `grant:<id>`+approved_by-null / `self:<user_id>` for landlord thread messages (stamped server-side, M2).

## Blockers
(none — proceeding to M1)

## Questions for coordinator
1. ~~`external_ref` on direct agent communications~~ — RESOLVED (INBOX ack; shipped in M0, firewall tightening landed with M2 as announced).
2. ~~`GET /comms/outbox/{id}`~~ — RESOLVED (INBOX: keep).
3. `thread_autonomy` / `voice_autonomy` policy `params` have no canonical shape yet — M2 passes them through unvalidated (only `rent_reminder` is strict per your INBOX note). Publish shapes when agreed and I'll tighten in a follow-up.
4. **Finding 1 (grant validation on the direct `POST /interactions` path).** Should an agent citing a `grant:<id>` when journaling a confirmed send be rejected if the policy doesn't exist / isn't ours? I did NOT enforce it (rejecting a revoked-since grant would suppress the record of a real send → ADR-0007 violation; and it changes behavior on Plan B's active path). If you want a nonexistent-grant guard here, I can validate EXISTENCE only (not active-status) once you confirm Plan B always emits a real `comm_policies` uuid in the grant ref. Your call.
4. Deploy sequencing reminder: migration `20260701000002_comms_ledger.sql` must be applied to prod BEFORE (or with) this branch's deploy — the M2 handlers and the rebuilt `interactions_with_chain` view depend on it. Also note the direct agent journal path stays enabled until you signal Plan B's M4, per your instruction.

## Events-feed entity_type strings (per INBOX ask — for the FE poller map)
`_emit_event` uses TG_TABLE_NAME verbatim, so the new audited tables emit exactly:
`comm_outbox`, `comm_threads`, `comm_thread_participants`, `channel_identities`, `platform_numbers`, `thread_channel_bindings`, `comm_policies`.
(`comm_opt_outs` and `inbound_raw` are deliberately NOT audited — no account_id; integrity via PK/UNIQUE. Delivery-state transitions arrive as `comm_outbox` `updated` events; journal appends as `interactions` `inserted` events, as today.)

## Design notes for B/C (final behavior)
- Transport flow: `GET /comms/outbox?status=queued&eligible_at=now` → `POST …/delivery {status:'sending'}` (claim) → dial provider → `POST …/complete {provider, provider_sid}` (or `…/fail`, `reconcile:true` when the outcome is unknown). Complete is idempotent on sid replay; complete works from `queued` directly too (claim recommended, not required).
- Opt-out enforcement is a BEFORE-INSERT trigger on comm_outbox → API 422 `error.code='opted_out'`: refused sends leave no journal trace. `record_opt_out` parks queued intents to that address as `undeliverable` (`error_code='opted_out'`) globally; `sending` rows are left for the provider to refuse. First opt-out wins; replays return the original row.
- Agent send intents under `approval_ref='grant:<id>'` are validated against a LIVE `comm_policies` row of that account (403 otherwise). Policy revoke parks its queued sends (`error_code='policy_revoked'`) and blocks new intents.
- Inbound: `disposition='opted_out'` means the message WAS journaled (the contact happened) but the transport must not relay/reply. `orphan` = raw-captured only, nothing journaled (no binding).
- Journal rows read via `/interactions*` and thread messages now expose derived `outbox_id` / `delivery_status` / `delivered_at` (chain-view join) and `thread_id`.

## Log
(newest first; one line per push: date, milestone, summary)
- 2026-07-03 E2-A2 ✅ (commit `d5efad2`): account-agnostic transport token-resolve read (RLS + agent-role double fence, uniform 404) + threads channel filter. NEW sha `280b05d4…` — needs verify + broadcast. 5 new checks (28 total); all gates green. Last core item before the email PR.
- 2026-07-03 E2-A ✅ (commit `cc7ca02`): email inbound + threads — per-(thread,participant) tokenized reply addresses (dedicated reply_address column, trigger-stamped binding channel, active-token partial-unique), token-routed capture w/ sender verification (NEW disposition `sender_mismatch`, flagged decision on attribution), channel-aware thread legs + email subject seed, relay unchanged-and-verified. NEW sha `0f0cb770…` — needs verify + broadcast. All gates green (23-check suite; full regression; F7 check updated). Migration `20260703000002` + `EMAIL_REPLY_DOMAIN` pending prod.
- 2026-07-03 STATUS-SYNC: E1-A VERIFIED + broadcast (both flagged items approved). No open work items — standing by for the cutover-verified signal (mailer deletion) or a new INBOX item.
- 2026-07-03 E1-A ✅ (commit `7bd6f73`): email slice 1 — subject (email-only, journal `Subject: …\n\n…`), `system:<flow>` provenance fenced to the service tier (+author_type response enum widened w/ 'system'), public HMAC unsubscribe (v1 token format documented for B; RFC 8058 one-click; parks + 422s like STOP), renewal cutover behind `COMMS_EMAIL_PIPELINE` (OFF; mailer intact until cutover-verified), scan `channel` filter. NEW sha `d6adb2b9…` — needs verify + broadcast. All gates green (18-check email suite; full regression). Migration `20260703000001` + 2 env vars pending prod.
- 2026-07-03 PR #50 POST-DEPLOY VERIFIED (01:34:47Z): live `/openapi.json` semantically == `304e32c2…` (raw `c739246e…` = minified form); all 4 GM-A fields live; 107 paths.
- 2026-07-02 GM-A ✅ (commit `d580c27`): native group-MMS threads — mode + DB-enforced group-set uniqueness (stored canonical routing key), `cc[]` set-match capture (account-pinned), one-outbox group sends (frozen recipient set, journal-once), any-member STOP (422 + park). NEW sha `304e32c2…` (strictly additive, 4 fields) — needs coordinator verify + broadcast. All gates green (comms 37 + group 24 + full db/api suites). Not on prod; GM batch merges via coordinator PR.
- 2026-07-02 STATUS-SYNC: reconciled to reality — coordinator ACCEPTED live verification (DoD met, comms live+dormant); `…05` APPROVED pending human apply + coordinator PR; GM-A (group-MMS) received, not started. Working tree clean (two stray pre-session files left untracked).
- 2026-07-02T23:10:44Z **LIVE DEPLOY VERIFIED**: `/openapi.json` 200, all 17 `/comms/*` ops live, contract semantically == committed. Live raw sha `7a4b764a…` = minified form of `7143b97f…` (NOT a mismatch — pretty-vs-minified). Deploy DoD MET.
- 2026-07-02 PERF+RETENTION: migration `…05` (opt-out-park index, dispatch-scan reorder, drop 4 redundant account_id indexes, `prune_inbound_raw` janitor) + `test:comms-retention`. DB-only, contract-neutral, no sha change. NOT yet on prod (needs a follow-up `migrate:up`). All gates green.
- 2026-07-02T22:30:55Z **PROD MIGRATION APPLIED**: human ran `migrate:up`; `…02`+`…03`+`…04` all applied clean ("Finished supabase db push"). Over to coordinator for PR-to-main + `/openapi.json` verify + broadcast.
- 2026-07-02T18:18Z PROD MIGRATION BLOCKED: GO received; attempted `migrate:up` → classifier denied prod creds (expected). Handed the one command to the human; prod untouched; awaiting human to apply + confirm.
- 2026-07-02 REOPEN batch ✅: migration `…04` (relay no-double-journal + `thread:` provenance + tenancy/mreq context) + spec item-3 additive fields. NEW sha `7143b97f…` — needs verify + re-broadcast. All gates green (comms 37). THREE migrations now pending. Still holding prod at your combined go/no-go.
- 2026-07-02 DEPLOY HOLD: review found real issues → holding at deploy-sequence step 1; prod untouched; awaiting human re-confirm + your re-broadcast of `097d9dc8…`. TWO migrations now pending (`…02` + `…03`).
- 2026-07-02 HARDENING ✅: adversarial review → migration `…03` (F1 evidence-honesty + F2/F5 cross-account + F3/F4 opt-out leak) + 7 API fixes incl. the `injectSchemaHygiene` corruption that broke `CommOutbox.status` in the broadcast sha. New FINAL sha `097d9dc8…` — **needs re-broadcast**. All gates green (comms 34 checks + full DB suite + unit 67 + drift×2). Finding 1 deferred to coordinator (Q4).
- 2026-07-02 FINAL ACK received — Plan A complete; contract `f3336606…` broadcast to B/C. Post-ack hardening review in progress locally.
- 2026-07-02 contract-hygiene ✅: all 3 INBOX re-emit items + CommPolicy.quiet_hours nullability; FINAL spec sha `f3336606…`; suite re-verified green (and made re-runnable against a persistent stack).
- 2026-07-02 M2+M3 ✅ (commit `12c851b`): all 17 handlers live + 24-check integration suite wired into CI; every gate green. Definition of done reached pending coordinator ack.
- 2026-07-02 M1 ✅: comms ledger migration + guard-allowlist + seeds; full local DB suite green (isolation/audit/money/tenancy/definer-guard).
- 2026-07-01 M0 ✅: firewall provenance gate + 17 comms stub endpoints + spec/SDK; gates green; contract commit `d77528f`.
