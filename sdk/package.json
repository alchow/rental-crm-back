{
  "name": "@rentalcrm/sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Generated TypeScript client for the /v1 contract. Regenerated from openapi/openapi.json; CI fails on drift.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "generate": "openapi-typescript ../openapi/openapi.json --output src/generated/types.ts",
    "build": "echo 'sdk build wired in a later phase' && exit 0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "openapi-fetch": "^0.13.8",
    "openapi-typescript": "^7.13.0"
  }
}
